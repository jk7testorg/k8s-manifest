apiVersion: v1
kind: Service
metadata:
  name: goldenimagetest3
spec:
  selector:
    app: goldenimagetest3
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer
