apiVersion: v1
kind: Service
metadata:
  name: example-repo-developer-15
spec:
  selector:
    app: example-repo-developer-15
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

