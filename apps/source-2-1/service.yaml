apiVersion: v1
kind: Service
metadata:
  name: source-2-1
spec:
  selector:
    app: source-2-1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

