apiVersion: v1
kind: Service
metadata:
  name: backstage-ssp-04
spec:
  selector:
    app: backstage-ssp-04
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

