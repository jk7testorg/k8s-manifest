apiVersion: v1
kind: Service
metadata:
  name: april-13-test-1
spec:
  selector:
    app: april-13-test-1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

