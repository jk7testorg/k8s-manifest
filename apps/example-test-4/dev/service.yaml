apiVersion: v1
kind: Service
metadata:
  name: example-test-4
  namespace: dev
  labels:
    backstage.io/kubernetes-id: example-test-4
    environment: dev
spec:
  selector:
    app: example-test-4
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP