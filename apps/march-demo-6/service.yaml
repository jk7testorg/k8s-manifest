apiVersion: v1
kind: Service
metadata:
  name: march-demo-6
spec:
  selector:
    app: march-demo-6
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

