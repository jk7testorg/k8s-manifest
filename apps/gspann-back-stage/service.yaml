apiVersion: v1
kind: Service
metadata:
  name: gspann-back-stage
spec:
  selector:
    app: gspann-back-stage
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

