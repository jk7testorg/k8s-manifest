apiVersion: v1
kind: Service
metadata:
  name: github-jenkins-argo-demo
spec:
  selector:
    app: github-jenkins-argo-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer
