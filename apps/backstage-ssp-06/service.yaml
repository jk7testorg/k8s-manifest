apiVersion: v1
kind: Service
metadata:
  name: backstage-ssp-06
spec:
  selector:
    app: backstage-ssp-06
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

