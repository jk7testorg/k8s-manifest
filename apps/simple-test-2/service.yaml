apiVersion: v1
kind: Service
metadata:
  name: simple-test-2
spec:
  selector:
    app: simple-test-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: ClusterIP

