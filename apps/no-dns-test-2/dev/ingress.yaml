apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: no-dns-test-2
  namespace: dev
  labels:
    backstage.io/kubernetes-id: no-dns-test-2
    environment: dev
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header X-Forwarded-Prefix /no-dns-test-2-dev;
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /no-dns-test-2-dev(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: no-dns-test-2
                port:
                  number: 80
