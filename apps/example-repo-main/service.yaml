apiVersion: v1
kind: Service
metadata:
  name: example-repo-main
spec:
  selector:
    app: example-repo-main
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

