apiVersion: v1
kind: Service
metadata:
  name: java-ssp
  namespace: dev
  labels:
    backstage.io/kubernetes-id: java-ssp
spec:
  selector:
    app: java-ssp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
