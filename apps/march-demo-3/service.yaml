apiVersion: v1
kind: Service
metadata:
  name: march-demo-3
spec:
  selector:
    app: march-demo-3
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer
