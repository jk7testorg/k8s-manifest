apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-python-test-2
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: / 
spec:
  rules:
    - http:
        paths:
          - path: /example-python-test-2(/|$)(.*)               
            pathType: Prefix        
            backend:
              service:
                name: example-python-test-2
                port:
                  number: 80
