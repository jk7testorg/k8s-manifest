apiVersion: v1
kind: Service
metadata:
  name: march-demo-5
spec:
  selector:
    app: march-demo-5
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

