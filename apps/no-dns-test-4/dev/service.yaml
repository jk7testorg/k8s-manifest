apiVersion: v1
kind: Service
metadata:
  name: no-dns-test-4
  namespace: dev
  labels:
    backstage.io/kubernetes-id: no-dns-test-4
spec:
  selector:
    app: no-dns-test-4
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
