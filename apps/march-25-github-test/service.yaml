apiVersion: v1
kind: Service
metadata:
  name: march-25-github-test
spec:
  selector:
    app: march-25-github-test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

