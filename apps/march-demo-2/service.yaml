apiVersion: v1
kind: Service
metadata:
  name: march-demo-2
spec:
  selector:
    app: march-demo-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer
