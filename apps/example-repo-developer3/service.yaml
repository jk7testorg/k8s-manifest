apiVersion: v1
kind: Service
metadata:
  name: example-repo-developer3
spec:
  selector:
    app: example-repo-developer3
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

