apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: libtest-3
  namespace: dev
  labels:
    backstage.io/kubernetes-id: libtest-3
    environment: dev
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /libtest-3-dev
            pathType: Prefix
            backend:
              service:
                name: libtest-3
                port:
                  number: 80
