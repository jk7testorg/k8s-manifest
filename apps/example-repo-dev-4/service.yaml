apiVersion: v1
kind: Service
metadata:
  name: example-repo-dev-4
spec:
  selector:
    app: example-repo-dev-4
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

