apiVersion: v1
kind: Service
metadata:
  name: example-repo-new-2
spec:
  selector:
    app: example-repo-new-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer

